//promise in javascript use to handle the asynchronous nature of javascript , it take a call back function as an argument and that take two arguemnents resolve and reject and resolve is used to resolve the promise and reject is used to reject the promise. The message which is thrown by resolve or reject passes into .then chain and we can do anything with that message and .catch chain is used to handle the error

//closures is a js property which gives the fucntion the ability to use its lexical environment which is where it was defined. that means we can use any variable that is defined outside the function means in the lexical environment . that is the property known as closure


//var is function scoped and let and const are block scoped .

//Informational responses (100 – 199)
// Successful responses (200 – 299)
// Redirection messages (300 – 399)
// Client error responses (400 – 499)
// Server error responses (500 – 599)

//useCallback is a React Hook used to optimize performance by memoizing functions. It's helpful when you pass functions to child components to prevent unnecessary re-renders.
//useMemo is used to memoize the result of an expensive calculation, so it only recalculates when its dependencies change. This can prevent unnecessary re-renders and improve performance in your app.